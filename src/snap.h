#ifndef SNAP_H
#define SNAP_H

#include <string>
#include <vector>

namespace snap {
  std::vector<int> find(std::string pattern, std::string s);
  /* int or(); */
  /* int not(); */
  /* int near(); */
  /* int and(); */
}

#endif

